[project]
name = "box"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
box = "box.cli:main"
pub_version = "flutter.pub_version:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
