[project]
name = "box"
version = "0.1.49"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
box = "box.cli:main"
box_pub_version = "box_tools.flutter.pub_version.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


[tool.hatch.build.targets.wheel]
packages = ["src/box", "src/box_tools"]

